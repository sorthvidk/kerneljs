var expect = require('chai').expect;
var kernel = require ('../dist/kernel');
console.log(kernel);

describe('First Kernel test', function(){
	it('should work', function(){
		expect(true).to.be.true;
	})

});
